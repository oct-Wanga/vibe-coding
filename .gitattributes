# 기본은 텍스트 자동 처리(플랫폼별)
* text=auto

# Windows에서 자주 쓰는 파일만 CRLF로 강제하고 싶다면
*.bat text eol=crlf
*.cmd text eol=crlf

# 쉘 스크립트는 LF 강제(깨짐 방지)
*.sh text eol=lf

# (선택) 설정/코드도 CRLF 강제하고 싶으면 여기서만 명시
# *.ts text eol=crlf
# *.tsx text eol=crlf
# *.js text eol=crlf
# *.json text eol=crlf
